@using Feature.User.Services.Chat
@inject ChatService ChatService
@implements IDisposable


<section class="bg-gradient-to-br h-full w-full flex-col overflow-hidden from-blue-500 to-blue-800">
	<HeaderChat />
	<BodyChat />
	<SendMessage />
</section>


@code {
	public void Dispose()
	{
		ChatService.OnChange -= StateHasChanged;
	}

	protected override async Task OnInitializedAsync() => ChatService.OnChange += StateHasChanged;
}